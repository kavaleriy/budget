= form_for @key_indicate_map_indicator do |f|
  - if @key_indicate_map_indicator.errors.any?
    #error_explanation
      %h2= "#{pluralize(@key_indicate_map_indicator.errors.count, "error")} prohibited this key_indicate_map_indicator from being saved:"
      %ul
        - @key_indicate_map_indicator.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
