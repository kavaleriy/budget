$('.photos').replaceWith('<%= j render partial: 'properting/properties/photos' %>');
var messages = $('#admin-flash')
messages.html('<%=  j (bootstrap_flash) %>').show();
setTimeout(function(){ messages.hide('slow') }, 2000);
