// used for show and edit actions
$('#properties').html('<%= escape_javascript(render partial: 'properties') %>');
$('.paginator-properties').html('<%= escape_javascript(paginate(@properties, remote: true)) %>');
