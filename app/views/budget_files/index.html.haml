- model_class = BudgetFile
= content_for :header do
  .head.pull-left
    %i.fa.fa-money
    #{t('.title')}

%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:title)
      %th= model_class.human_attribute_name(:file)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @budget_files.each do |budget_file|
      %tr
        %td= link_to budget_file.id, budget_file
        %td= budget_file.title
        %td= budget_file.file
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_polymorphic_path(budget_file), :class => 'btn btn-default btn-xs'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), polymorphic_path(budget_file), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs btn-danger'

= link_to t('.upload_revenue'), revenues_upload_path, :class => 'btn btn-primary'
= link_to t('.upload_expense'), expenses_upload_path, :class => 'btn btn-primary'
-#= link_to t('.new_revenue'), new_revenue_path, :class => 'btn btn-primary'
= link_to t('.upload_budget_file'), budget_files_upload_path, :class => 'btn btn-primary'
