- if @countdown_event and @countdown_event.ends_at > Date.current
  .countdown.col-sm-12
    = link_to(event_path(@countdown_event, { :calendar_id => @calendar.id }), :class => '', :remote => true) do
      %h4.title.col-sm-7
        #{@calendar.countdown_title.html_safe}
      .date.col-sm-5{ 'data-date' => @countdown_event.ends_at}


  :javascript
    $(function() {
      $('.countdown .date')
        .TimeCircles({
          circle_bg_color: "#CCCCCC",
          fg_width: 0.03,
          time: {
            Days: {
              show: true,
              text: "Днів",
              color: "#FF9933"
            },
            Hours: {
              show: false,
            },
            Minutes: {
              show: false,
            },
            Seconds: {
              show: false,
            },
          }
        });
    })
