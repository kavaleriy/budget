:css
  .map_container {
    height: 700px;
  }


= content_for :top do
  .map_container
    = render partial: 'map'



:javascript

  $(document).ready(function() {

    var aRepairMap

    $.ajax({
      method: 'get',
      url: '#{public_ukraine_geo_json_path}',
      dataType: 'json',
    })
    .done(function (data) {
      aRepairMap = RepairMap({ clusterMarkers: true })
      aRepairMap.addGeoJson(data)
    })
    .fail(function () {
    });

  });
