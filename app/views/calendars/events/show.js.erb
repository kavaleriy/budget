<% if current_user %>
    $(".calendar_event[owner=<%= @event.holder %>]").html("<%= escape_javascript(render partial: 'show') %>");
<% else %>
    $(".calendar_event[owner=<%= @event.holder %>]").html("<%= escape_javascript(render partial: 'show_guest') %>");
<% end %>