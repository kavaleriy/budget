<% if current_user %>
$("#calendar_event").html("<%= escape_javascript(render partial: 'show') %>");
<% else %>
$("#calendar_event").html("<%= escape_javascript(render partial: 'show_guest') %>");
<% end %>