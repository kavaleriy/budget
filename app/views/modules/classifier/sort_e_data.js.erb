$('#edata_payments tbody').html("<%= escape_javascript(render partial: 'search_e_data_results') %>");
$('#pagination .pagination').html("<%= escape_javascript(paginate(@payments, remote: true, theme: 'e_data_template', params: { 'sort_col' => params['sort_col'], 'sort_dir' => params['sort_dir'] })) %>");