= content_for :header do
  .head.pull-left
    Державні програми


.col-sm-offset-2.col-sm-7
  = render partial: 'programs_bar'

  = render partial: 'programs_list', locals: { programs: @programs, :years => @years, :year => @year }
