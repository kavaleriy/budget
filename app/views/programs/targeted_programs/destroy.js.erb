$('.btn-delete').bind('ajax:success', function() {
  $(this).closest('tr').fadeOut();
  // bild msg
  $(".bootstrap_flash").append($("<div class='alert fade in alert-success alert-dismissable'>").html("<%= flash[:success].html_safe %>"));
});
