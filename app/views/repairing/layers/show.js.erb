// used for show and edit actions
$('#repairs').html('<%= escape_javascript(render partial: 'repairs') %>');
$('.paginator-repairs').html('<%= escape_javascript(paginate(@repairs, remote: true)) %>');