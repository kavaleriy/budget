.col-xs-12
  = bootstrap_form_for location, :html => { :class => 'form-horizontal' } do |f|

    = f.text_field :title, label: t('taxonomies.index.file_title')

    = f.submit nil, :class => 'btn btn-primary'
