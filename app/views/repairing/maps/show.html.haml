= content_for :header do
  .head
    %i.fa.fa-map
    #{@repairing_map.title}

  .col-xs-4.pull-right
    = render partial: 'search'

= render partial: 'map'

= render partial: 'repair', :locals => { repair: @repairing_map.repairs.new }


= link_to 'Edit', edit_repairing_map_path(@repairing_map)
\|
= link_to 'Back', repairing_maps_path
