:css
  #category_description span, #category_title {
    font-size: 1.2em;
    font-weight: bold;
  }
  #category_description span {
    color: #428bca;
  }
  #category_title {
    font-size: 1.4em;
  }
  #category_content {
    margin-top: 10px;
  }
  .leaflet-top.leaflet-right {
    width: 300px;
  }

  #s2id_map_town {
    font-size: 1.2em;
  }

  #s2id_map_town .select2-choice {
    height: 34px !important;
  }

  #category_btns .btn {
    padding: 4px;
  }

#category_description
  %span
    Вибрати категорію робіт:
#category_btns.btn-group
  - Repairing::Category.all.select{|p| p.category.nil? }.each do |category|
    .btn.btn-default{:title => category.title, 'data-id' => category.id}
      = image_tag category.img? ? category.img.thumb.url : 'repairing/default_thumb.png'
#category_content
  #category_title
  #subcategories

- if @town.blank?
  .form-group
    = text_field_tag 'map_town', ''
.input-group
  %span.input-group-addon
    %b рік
  = select_tag 'year_select', options_for_select([nil] + ((Time.now.year - 4)..Time.now.year).to_a.reverse), :class => 'form-control'